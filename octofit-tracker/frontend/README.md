# OctoFit Tracker — Frontend

The frontend will be a React app that communicates with the Django REST API.

Suggested setup:

```bash
# from repo root
cd octofit-tracker/frontend
npm init vite@latest . --template react
npm install
npm run dev
```

Keep frontend code in this directory and follow React + ESLint/Prettier conventions.
